en:
  activerecord:
    models:
      test: "Test"

    attributes:
      test:
        title: "Название"
        category: "Category"

    errors:
      messages:
        blank: "can't be blank"
        required: "required"
